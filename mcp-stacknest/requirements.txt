# Upper bound is deliberate, and stays deliberate. An unbounded ">=1.2" is what
# let 2.0.0 arrive on its own and break the server on import - mcp 2.0 removed
# mcp.server.fastmcp.FastMCP (2026-08-08). server.py now uses the 2.x successor
# mcp.server.mcpserver.MCPServer, so the bound moves rather than disappears:
# the point of a bound is to make a major upgrade a decision, not an accident.
# Do not remove it. Raise it only after checking server.py against the new API.
mcp>=2,<3
