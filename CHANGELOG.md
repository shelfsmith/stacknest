# Changelog

**日本語** | [English](CHANGELOG.en.md)

本ファイルは StackNest の主な変更を記録します。形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準じ、バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います。
リリースは自己署名（匿名 CN `StackNest Self-Signed`・固定 identity で TCC 権限が持続・Apple 公証なし）の Universal ビルドで、[Releases](https://github.com/shelfsmith/stacknest/releases) から配布します。

> **バージョン付けについて:** タグ付きリリースは `0.8.0` から始まります。それ以前はフェーズ単位（2.1〜2.6）で開発しており、明確なバージョン番号は振っていません。本ファイル末尾の「0.8.0 以前（フェーズ管理期・タグなし）」にフェーズごとの履歴をまとめています。

## [0.12.0] - Unreleased — 共有トークン / ヘッドレス CLI・MCP / 監視取込 / リモート永続キャッシュ / 外部表紙 / リモート parity 締め / 内蔵ビューア安定化（Phase 4.2d–4.2f・C・G3・G4・G12b・G15・G16）

> プレリリースとして配布: `v0.12.0-rc.1`（2026-07-07）/ `v0.12.0-rc.2`（2026-07-18）。0.11.0（Phase 4.2）以降に積み上がった変更をまとめています。共有の権限分離（トークン）、コマンドライン / AI エージェントからの操作、フォルダ監視の自動取込、リモート閲覧のディスク永続キャッシュ、外部画像を表紙に、に加えてリモート運用の仕上げ（監視フォルダタブ・admin メンテナンス・取り消し/やり直し）と内蔵ビューアの安定化などが加わりました。

### Added

**共有・権限**
- **共有トークン（アクセスティア）**: 相手ごとに **read / edit / admin** の権限と、見せるライブラリ（scope）を分けたトークンを発行できます。サーバ設定にトークン一覧（名称変更）・**トークン別の QR / URL** を追加。
- **トークンのライブ反映**: トークンの発行 / 名称変更 / 再生成 / 無効化が**サーバ再起動なしで即時反映**されます（旧トークンは即失効）。
- **リモートで本を削除（admin）**: 管理トークン接続時、リスト / グリッドの右クリックからリモートのまま「ライブラリから削除」「ゴミ箱へ」。

**自動化（CLI / MCP / API）**
- **ヘッドレス CLI `stacknest-cli`**: ローカル / リモートのライブラリをコマンドラインで操作（`libraries` / `list`（フィルタ・ブラウズ・ソート・scope・fields）/ `add` / `rm` / `set` / `detail` / `facets` / `shelves` / `me` / `shelf` CRUD / `watch` / `lock`（パスワードは stdin）/ `import` / `relink` / `dedup` / `unlock` / `grant` / `stamp` / `label`）。アプリに同梱し、ローカルアクセス用の使い捨てトークンで接続。
- **MCP サーバ `mcp-stacknest`**: 上記 CLI を包む Model Context Protocol サーバ。AI エージェントからライブラリの閲覧・編集・取り込み・共有トークン管理などを実行できます。
- **OpenAPI 3.1 + Redoc**: ローカルエンドポイントを API 専用化し、`/` に Redoc の API ドキュメントを同梱。

**取り込み**
- **監視フォルダ（自動取込）**: 指定フォルダを監視し、置かれたアーカイブ / 画像フォルダを自動でライブラリへ取り込み（フォルダ毎の命名プリセット・初回プレビュー・取込要約バナー）。
- **per-library 取り込み設定**: 自動分類（bookType）・厚さ閾値をライブラリごとに上書き（「StackNest 設定の値に従う」/ 個別指定）。設定に「取り込み」タブを追加。

**リモート閲覧の高速化・追従**
- **リモートリーダーのディスク永続キャッシュ**: リモート閲覧のページ / 表紙を端末にディスク永続キャッシュ（LRU + 可視保護 + TTL）。**再接続 / 再起動後もキャッシュが効きます**。設定「リモートキャッシュ」で上限 / 保持期間 / 使用量 / クリアを管理。
- **先読みの Web パリティ + 全冊先読み**: 内蔵ビューワの先読みを「前方優先 + スキップストライド」に刷新。放置で本 1 冊分をキャッシュする**全冊先読み（tier3）トグル**（既定オフ）。
- **キャッシュ カバレッジ帯**: リモートビューアのプログレスバーに、L2 キャッシュ済みのページ範囲を淡色帯で可視化。
- **表紙キャッシュのサーバ追従（G4c）**: サーバの版トークンで表紙キャッシュを版鍵化し、リモート閲覧クライアントが**サーバ側の表紙変更へ一覧リロード / 再接続で追従**（手動キャッシュ削除が不要に）。ホストアプリはリモート由来の表紙変更を開き直さず反映。

**表紙**
- **外部画像を表紙に（ローカル G4a / リモート G4b）**: 詳細ペインの表紙へ**画像をドラッグ&ドロップ**、または「外部画像を表紙に設定…」メニュー→クロップで、任意の外部画像を表紙に設定。**見た目のみの変更でアーカイブ本体は不変**。RW トークンのネイティブリモートクライアントでも利用可（サーバに `PUT /books/:id/cover-image` を追加）。

**リモート編集・Web**
- **レート / 未読を R トークンでも編集**: 共有評価 / 閲覧状態として、閲覧のみのトークンでもレート・未読を変更可（サーバ同期）。
- **サーバ同期のラベルカスタマイズ（リモート）**: リモートの詳細 / ファセット / スタンプ表示がサーバ同期のカスタムラベルを反映。RW でラベル編集も可能。
- **Web リーダー巻末ナビ + PWA**: Web リーダーに巻末ダイアログ（次の巻へ / 先頭へ / 閉じる）、favicon / apple-touch-icon / PWA manifest を追加。
- **起動時オプション**: 「前回開いていたライブラリ」で**複数窓を全復元**、起動時にリモート共有を**自動 ON** にするオプション（著作権同意ゲート付き）。

**リモート運用の仕上げ（G12b）**
- **監視フォルダのタブ化 / サブフォルダ取込モード修復**: リモート設定シートの監視フォルダ編集を「取り込み」タブに統合。保存時にサブフォルダの取込モード（subfolderMode）が失われる不具合を修正。
- **管理者メンテナンス（進捗表示付き）**: メタデータの補完・表紙の圧縮を、進捗バー付きの非同期ジョブとしてリモートから実行可能に。
- **リモートの取り消し・やり直し（⌘Z / ⌘⇧Z）**: メタデータ編集・削除をリモートから元に戻す／やり直せるように。
- **命名フォーマットプリセットのリモート編集**: プリセットの追加・変更・削除を対応ティアからリモートで実行可能に。
- **監視フォルダの既存ファイル一括再取込**: 「既存も取り込む」で、監視開始前からあったファイルもまとめて取り込み。

**内蔵ビューアの安定化（G15）**
- **同一本の重複起動を防止**: ローカル・オフライン・リモートいずれでも、読み込み中の同じ本を続けて開こうとしても複数ウィンドウにならないよう起動処理を刷新。設定に「**複数ビューアの起動を許可**」を追加（既定オフ＝別の本を開くと既存ウィンドウを再利用、オンで本ごとに別ウィンドウ）。
- **リモートグリッドの無表紙表示**: 表紙の無い本が読み込み中のまま回り続けず、すぐに無表紙アイコンを表示。
- **非対応形式の専用メッセージ**: リモートで動画・文書など内蔵ビューア非対応の形式を開こうとした際、汎用エラーではなくカテゴリ別のメッセージを表示。

### Changed
- **共有トークンに一本化**: 旧「アクセス / 編集トークン」UI を共有トークン一覧へ統合。ユーザー向け文言を「共有トークン」に統一。
- **ローカルアクセス設定をアプリ設定へ**: ローカル制御（CLI / MCP 用）の設定を共有窓からアプリ設定タブへ移設し、「ローカルアクセス」に改称。
- **設定タブ再編**: 一般 / 取り込み / 表示 / ビューアキー に整理（取り込みタブに自動分類 + 監視フォルダを統合）。
- **共有前の著作権警告**: サーバ開始前に著作権警告（以後抑止可）を表示。
- UI 表記「リモート / オフラインビューア」→「**リモート / オフラインブラウザ**」に統一。
- **無限スクロールのライブ同期を並列化**: リモートブラウザの追従取得をまとめて並列取得するよう変更し、体感を改善。

### Fixed
- **リモート表紙の stale**: リモートで表紙を差し替えても、編集した詳細ペインや別の閲覧クライアントが古い表紙のまま残る問題を修正（表紙取得の HTTP キャッシュ〔`immutable`〕を差し替え時にバイパス / 表紙キャッシュをサーバ版で版鍵化 / ホストは per-book で反映）。
- **起動復元で閉じた窓が復活**する問題を修正（`NSWindow.willClose` 増分方式へ変更・終了フック非依存）。
- **複数窓での削除確認が 2 重表示**される既存バグを修正（通知のフォーカスガード）。
- **トークンの失効**: 再生成 / 無効化で旧トークンが失効しない・全トークン削除後に復活する問題を修正（既定グラントを現トークンへ同期・一度きり移行マーカー）。
- 内蔵ビューワ / サムネイルに **EXIF 回転**を反映。監視パス不在の警告・ポート競合時の自動再採番。
- **セキュリティ**: CLI のパスワードを stdin 入力に（argv 露出回避）・MCP add の引数スマグリング防止（`--`）・ローカルエンドポイントの Web UI 撤去（API 専用化）。
- **リモートの取り消し（undo）が同時編集を上書きしない**: メタデータ編集の取り消しが、他クライアントの並行編集を巻き戻さず正しい編集前の値へ戻るように修正。
- **ゴミ箱へ移動した本の取り消しでファイルも復元**: リモートで削除した本を ⌘Z で戻すと、登録だけでなくファイルもゴミ箱から元の場所へ復元されるように修正。
- **巻送り後の重複ウィンドウを抑止**: 内蔵ビューアで巻を送った直後、同じ本が別ウィンドウで重複して開くことがある問題を修正。
- **絞り込み時に一覧が先頭へ戻るように修正**: 一覧を大きくスクロールした状態で検索・絞り込みを行うと元の位置に留まっていた問題を修正（編集後やライブ同期による更新では位置を保持）。
- **セキュリティ**: リモートの復元操作を悪用した任意ファイル移動を防止。
- 削除確認ダイアログの文言を、⌘Z で取り消せることを反映するよう整合（リモート削除の確認も含む）。
- 未設定（空欄）のタグ欄に値を入力した後の取り消し（⌘Z）で欄が正しく空へ戻るよう修正。

## [0.11.0] - Unreleased — リモート共有 / ネイティブクライアント / オフライン / リモート編集（Phase 4.2）

> プレリリースとして配布中: `v0.11.0-rc.1`（2026-06-14・Phase 4.2b＝閲覧 / オフライン基盤）/ `v0.11.0-rc.2`（2026-06-21・Phase 4.2c＝同等化・リモート編集）。

### Added
- **ネイティブリモートクライアント**（4.2b-1b-2b）: 別 Mac の StackNest から共有サーバへ接続し、サイドバー / ファセット / フィルタ / グリッド・リスト / 詳細ペインでフルブラウズ＋内蔵ビューワで閲覧。読書進捗はサーバへ同期。サーバに `/shelves`・`/facets`・`/books/:id/detail`・`scope+filter+browse` 付き `/books` を追加。
- **オフラインダウンロード**（4.2b-2）: リモートの本をローカル保存（`OfflineStore` + `index.json`）し、**接続なし**で一覧・閲覧（続きから）。タイトル画面 / File メニューの「オフライン（ダウンロード済み）」から起動。
- **一括ダウンロード / 削除**（4.2b-5・4.2c-3）: リモート / オフラインで ⌘ / Shift による複数選択 → 一括 DL / 一括削除（ネイティブ複数選択へ統一）。
- **リモート / オフライン巻送り**（4.2b-4・4.2b-6）: 内蔵ビューワの前 / 次の巻がリモート（隣接巻ストリーム・未 DL 可）・オフライン（連続巻）でも動作。読みかけの巻は「続き / 最初」を選択。
- **リモート検索**（4.2c-3）: ツールバー検索（ライブフィルタ＋300ms デバウンス＋クリア）。
- **リモート編集（RW トークン）**（4.2c-6a・4.2c-6b）: 詳細ペインでメタデータの単一 / 複数選択一括編集（進捗 N/M ＋中断）・スタンプ付与 / 定義編集・**表紙ページ選択 / クロップ編集**・読み方向変更。スタンプ定義はサーバ常駐 canonical で同期、表紙はサーバ側で thumbnail 再生成。
- **リモートブラウザ状態の永続化**（4.2c-7）: ファセット / ソート / grid / フィルタ / サイドバーを (サーバ, ライブラリ) 単位で UserDefaults に保存（別ライブラリ非伝播）。
- **keywordC フルカラム化**（4.2c-6c）: ローカル / リモートのリスト列・ソートに keywordC を追加（リモート複数選択編集の現在値表示も対応）。

### Changed
- **リモートグリッドをローカル同等化**（4.2c-4）: ⌘ / Shift 複数選択・表示サイズスライダー・右クリック並び替え・ツールバー配置をローカルと統一。ローカルの「シリーズ」並び替えもシリーズ→巻数の 2 段ソートに統一。
- **リモートビューア進捗の二重同期**（4.2c-5）: リモート（DL 済み）とオフラインの進捗を双方向 write-sync し、開く時は最大ページで解決。
- README（日本語 / 英語）・アプリ内ヘルプを更新し、リモートの共有 / クライアント / オフライン / 編集（RW 編集対応）を記載。

### Fixed
- リモート / オフラインの多数の UX 修正（一覧の単一クリック選択・詳細ペイン幅固定・DL 中の中断 / 進捗 / 選択巻き戻り・ビューア続きから再開・「読んだ日」列の更新・Web 検索の IME 合成中の誤発火 ほか）。
- 表紙差し替え後にリモートグリッドが古い表紙のまま残る問題、リモートグリッドのクロップ未適用を修正（`coverVersion` / `coverCropRectJSON` を一覧 DTO に追加）。
- セキュリティ: サーバ応答から path を秘匿（漏洩防止）・読み取り専用時に「Finder で表示」を非表示・ファセット / browse 列識別子の allowlist 化（SQL injection 対策）。

## [0.10.0] - 2026-06-08 — ファイル名コピー & リリース整備（B24）

### Added
- **右クリック「ファイル名をコピー」（B24）**: グリッド/リストの本の右クリックメニューに追加。拡張子なしのファイル名をクリップボードへコピー（単一選択時のみ有効、複数選択・path 無しはグレーアウト）。

### Changed
- リリースを正式版として配布（prerelease 解除）。
- インストール手順を **macOS 15 Sequoia 以降の Gatekeeper 仕様**に合わせて更新（「右クリック →『開く』」は廃止 → システム設定の「セキュリティ」から「このまま開く」で許可、または `xattr` で quarantine 除去）。

## [0.9.0] - 2026-06-07 — ライブラリ保全 & DB 修復（Phase 2.8 / 2.9）

### Added
- **DB 予防保全（B22）**: 編集のあったセッションを閉じるたびに、バンドル内 `Backups/` へ世代バックアップを自動取得（SQLite Online Backup API・閲覧のみの無編集セッションはスキップ）。保持世代数はライブラリごとに 1〜20（既定 5）で設定可能。
- **整合性チェック（B22）**: ライブラリを開く際に `PRAGMA quick_check` を実行し、破損を検知すると「最新の正常なバックアップから復元しますか？」と案内。設定に手動の整合性チェック／今すぐバックアップ／バックアップフォルダ表示ボタンを追加。
- **アプリ内 DB 修復 `.recover`（B23）**: バックアップからの復元ができない場合、「.recover で修復を試す」からシステムの `sqlite3 .recover` を実行して可能な範囲のデータを救出。救出できた本の件数を提示し、正常なら差し替えて開く（壊れた本体は `library.corrupt-*` / `library.prerecover-*` として保持）。
- **リンク切れ再指定（A19）**: 本体ファイル／フォルダの移動で参照切れになった本を再リンク。右クリック「ファイルを再指定…」（即時）と、メニュー「リンク切れを検出…」（個別列挙＋フォルダ一括再マップ）。
- **復旧ガイド**: `docs/recovery-guide.md` を追加（手動 `sqlite3 .recover` 手順・退避ファイルの説明）。

### Fixed
- **NFC 正規化**: macOS のファイル名（NFD）由来のシリーズ／タイトルが、入力・取り込み由来（NFC）と別文字列として扱われ、ブラウズのファセットが分裂したりフィルタで取りこぼす不具合を修正。保存時に NFC 正規化し、既存データは migration v16 で一括バックフィル。

## [0.8.0] - 2026-06-06 — ブラッシュアップ & 性能（Phase 2.7）

### Added
- **重複検出（A20 / B11）**: 別ディレクトリにある同一内容の本を SHA-256 のバイト一致で検出（＋ series+volume 一致）。解決シート（登録のみ削除／ファイルもゴミ箱／グループ無視）。
- **ラベルカスタマイズ（A22 / A23）**: 内容系フィールドと bookType の表示名をライブラリごとに任意の名前へ。全表示箇所（列ヘッダ・ソート・詳細・フィルタ・スタンプ・ブラウズ・スマートシェルフ）へ一貫反映。
- **ビューワキー再割当 UI**: ビューワの全操作キーを設定「キー」タブで自由に割り当て（競合は拒否・ヘルプ自動生成）。
- **複数命名フォーマットプリセット（B6）**: 命名フォーマットを複数保存し、リネーム時に選択。
- **配布**: CI（GitHub Actions）による ad-hoc 署名 Universal ビルドのリリース配布を開始（初の tag 付きリリース）。

### Changed
- **ソート性能最適化（B9）**: DSU + ICU sort key（`ucol_getSortKey`）で大規模ライブラリのソートを高速化（実スケール 5,000 件で起動時の体感を大幅改善）。

---

## 0.8.0 以前（フェーズ管理期・タグなし）

タグ付きリリース以前の開発履歴をフェーズ単位で記載します（**新しい順**）。各フェーズの詳細設計は計画リポジトリのスペックにあり、本リポジトリの README「ロードマップ」が要約です。
（早期フェーズの日付は計画リポジトリの最終 smoke／決定ログに基づきます。2.2 は単独の完了記録が無く、関連 spec〜直後フェーズの smoke から 2026-05-03〜07 の完成と推定。）

### Phase 2.6g — 2026-06-04 — Keychain 非依存の生体認証ロック
- armedHash 方式で解錠経路から Keychain アクセスを排除（毎回プロンプトの根治・平文非保存）。

### Phase 2.6f — 2026-06-04 — ドキュメント & アプリ内ヘルプ
- README（日英）／CONTRIBUTING 整備、Help メニューを実装内ヘルプページ化。

### Phase 2.6e — 2026-06-04 — リブランド & 匿名化
- 識別子整理（`app.shelfsmith.stacknest`）と GitHub 移管（`shelfsmith`）。

### Phase 2.6d — 2026-06-04 — 安全性 & クリーンアップ
- マルチ Mac 同時オープン防止のロックファイル＋旧称クリーンアップ＋アプリ内 Help／About。

### Phase 2.6c — 2026-06-03 — 初回起動ウィザード
- 内蔵／外部ビューワ選択・内蔵初期設定・最初のライブラリ作成・設定から再表示。

### Phase 2.6b-2 — 2026-06-02 — 内蔵ビューワ拡張
- 見開き表示・本ごとページ方向・スライドショー自動進行・続きから読む・巻末挙動（次の巻／ループ）・全画面で開く・HEIC/HEIF/TIFF/AVIF 対応。

### Phase 2.6b — 2026-06-01 — 内蔵ビューワ コア
- 専用ウィンドウ・全画面・フィット／ズーム／パン・ページ送り・PDF／アーカイブ／フォルダ／単一画像。

### Phase 2.6a — 2026-05-31 — スマートシェルフ MVP
- 条件式コレクション（ルールリスト・動的評価・facet/FTS と AND 重ねがけ）。

### Phase 2.5k — 2026-05-29 — キーボードナビゲーション
- グリッド／リストの矢印移動・範囲拡大・Enter で開く・Home/End/PageUp/Down 統一。

### Phase 2.5g–2.5i — 2026-05-28 — 自動分類・表紙パイプライン・PDF（v0.6.5）
- bookType 自動分類の設定化（新規取込のみ・閾値設定・手動リラベル併用）。
- 表紙の crop UI（横長カバー対応・縦長正規化）＋表紙圧縮（保存時ダウンサンプル・全表紙再生成）。
- PDF 表紙取込・PDF ページ数認識（zip 内 PDF 含む）、単独 image/PDF/movie/text の追加。

### Phase 2.5f — 2026-05-27 — 「最近開いたライブラリ」経路集約
- 更新経路を 1 か所に集約（fixedLibrary 起動／Finder ダブルクリックでも履歴更新）。

### Phase 2.5e — 2026-05-27 — 拡張子別 外部ヘルパー
- 拡張子別ヘルパーマッピング／スライドショー・Zip ヘルパー個別指定／フォルダを外部ヘルパーへ渡す。

### Phase 2.5（コア）— 2026-05-24 — マルチライブラリ & ファイル操作
- 複数ライブラリ＋任意位置（Cmd+O・最近開いた一覧・macOS タブ）、ライブラリごとのパスワードロック＋Touch ID、画像フォルダ書籍（zip 化なし）、ファイル移動／リネーム（双方向トークン・プレビュー）、ファイル名パーサ修正、シリーズ＋巻数分離（migration v10）、表紙編集（D&D＋zip 内ページ選択）、削除安全化（Undo）、詳細ペイン Tab ナビゲーション。

### Phase 2.4 — 2026-05-09 — 編集 & フィルタ
- 詳細ペイン編集（タグドロップダウン・メモ・bookType）、ツールバー 3 種フィルタ（未読／レート／bookType）、ファセットフィルタ列（ブラウズ）、FTS5（trigram）全文検索、スタンプ表示、タグ「→」ジャンプ。

### Phase 2.3 — 2026-05-07 — ブラウザコア
- サイドバー（ライブラリ／お気に入り／最近の項目）、手動シェルフ、詳細ペイン（表示）、インクリメンタル検索、リスト表示、レート／未読／bookType 設定（メニュー＋ショートカット）、お気に入りへの D&D。

### Phase 2.2 — 2026-05-03〜07 — 表示の基礎
- グリッド／アイコン表示、サムネイル生成、アーカイブ（zip/cbz/cbr/7z）読み込み、外部ビューワ指定。

### Phase 2.1 — 2026-05-02 — 互換インポータ（最初の土台）
- Stackroom のライブラリ XML を解析して SQLite（GRDB）へ取り込む CLI インポータ。データモデル（タイトル／作者／ジャンル／キーワード／関連／メモ／レート／未読／ページ数／表紙／bookType）を確立。

[0.10.0]: https://github.com/shelfsmith/stacknest/releases/tag/v0.10.0
[0.9.0]: https://github.com/shelfsmith/stacknest/releases/tag/v0.9.0
[0.8.0]: https://github.com/shelfsmith/stacknest/releases/tag/v0.8.0
