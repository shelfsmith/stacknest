# Changelog

**日本語** | [English](CHANGELOG.en.md)

本ファイルは StackNest の主な変更を記録します。形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準じ、バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います。
リリースは自己署名（匿名 CN `StackNest Self-Signed`、固定 identity で TCC 権限が持続、Apple 公証なし）の Universal ビルドで、[Releases](https://github.com/shelfsmith/stacknest/releases) から配布します。

> **バージョン付けについて:** タグ付きリリースは `0.8.0` から始まります。それ以前はフェーズ単位（2.1〜2.6）で開発しており、明確なバージョン番号は振っていません。本ファイル末尾の「0.8.0 以前（フェーズ管理期、タグなし）」にフェーズごとの履歴をまとめています。

## [0.15.0] - 2026-09-26 — EPUB を読みやすく（本全体の進捗・続きから・同じ窓での巻送り）、ページ送りの演出、EPUB 用の外部ビューア（Phase G54）

> EPUB を毎日の読書に使えるところまで整えた版です。本全体のどこを読んでいるかが分かり、途中から開くかを選べ、
> 次の巻へ進んでも窓が閉じなくなりました。外部ビューアは EPUB を画像と別に選べ、オフラインの本も外部ビューアで開けます。
> 設定には「内蔵ビューア」タブを新設し、ビューアの設定とキー割り当てをまとめました。

### Added

- **★ EPUB の窓に本全体の進捗**（「N / M」と進捗バー）を出すようにしました。画像ビューアと同じ表示で、開いた直後や
  ページ送り・マウスの移動で出て、少しすると消えます。本全体のページ数を測り終えるまでは「**計測中…**」と出し、
  バーは章の位置からの概算になります。
  - 計測中に数字キーで割合ジャンプすると、章単位で移動したことを「計測中のため章単位で移動」と知らせます。
  - 各ページの下に出ていた章ごとのページ番号（ノンブル）は既定で隠しました。設定 ▸ 内蔵ビューア の
    「**EPUB のノンブルを表示**」で戻せます。
- **★ 途中まで読んだ EPUB を開くと「続きから読みますか？」**（続きから / 最初から）と訊くようにしました
  （画像の本と同じ）。本の先頭のままなら訊きません。
- **★ EPUB の巻送りが同じ窓の中で進む**ようになりました（これまでは窓を閉じて開き直していました）。
  次の巻に読みかけの位置があれば「続きから / 最初から」を訊きます。
  - 次の巻がテキストの EPUB なら、**画像ビューア（zip など）からでも EPUB の窓へ**切り替わります
    （これまでは「次の巻を開けません（0ページ）」で止まっていました）。EPUB の窓から画像の巻へも切り替わります。
  - 切り替えたときの全画面は**切り替え先の設定**（「全画面で開く（画像）」/「全画面で開く（EPUB）」）に従います。
    全画面どうしの切り替えでは、前の窓が全画面を抜け終わるのを待ってから開くので、普通の窓が一瞬出たり警告音が
    鳴ったりしません。
- **★ ページ送りの演出**を選べるようになりました（設定 ▸ 内蔵ビューア ▸「**ページ送りの演出**」: なし / フェード /
  スライド。既定は「なし」）。**画像ビューアと EPUB の両方に効く** 1 つの設定です。隣のページへの送りとスライドショーに
  掛かり、ジャンプや巻送りには掛かりません。キーを押し続けているときと「視差効果を減らす」が有効なときは省きます。
- **EPUB の配色**を選べるようになりました（設定 ▸ 内蔵ビューア ▸「**EPUB の配色**」: システムに合わせる / ライト / ダーク）。
- **表紙編集の入口を形式ごとに広げました。**
  - 「表紙を編集」が **EPUB と PDF** でも使えます（EPUB は中の画像、PDF はページから選びます）。リモートでも同じです。
  - 動画は右クリックの「**シーンから表紙を選択…**」（0.14.3 の「動画からシーンを選ぶ…」を改名）で、選んだシーンを
    **続けて切り抜け**ます。切り抜かずに「決定」を押せばシーン全体が表紙になります。
  - 単独の画像ファイルでは「表紙を編集」を押せない表示にしました（押しても「未対応のフォーマットです」と出るだけでした）。
- **EPUB の題名を取り込みに使う**設定を追加しました（設定 ▸ 取り込み ▸「**EPUB の題名を使う**」。既定は OFF）。
  ライブラリごとの設定でも上書きでき、CLI / MCP からも設定できます。取り込むときにだけ効きます
  （これまでは EPUB の題名は使われず、ファイル名から作った題名になっていました）。
- **Web リーダーのテキスト EPUB**:
  - 画像 1 枚だけのページ（表紙・挿絵）を**余白なしで画面いっぱい**に表示します（zip の本と同じく、余った部分は背景色）。
  - 本の詳細で、読みかけなら「**続きから読む / 最初から**」を選べます（zip の本と同じ）。
  - 上下のバーを zip の本と同じくページに重ねて表示し、**画面の中央をタップすると出し入れ**できます。
- リモート庫の**一覧（表）表示の右クリックにも「オフラインから削除」**を追加しました（これまではグリッド表示にだけありました）。

### Changed

- **★ 外部ビューアを EPUB だけ別に選べる**ようになりました。
  - 設定 ▸ 表示 ▸ ビューア で、内蔵 / 外部を「**画像**（アーカイブ・フォルダ・PDF・画像ファイル）」と
    「**電子書籍（EPUB）**」で別々に選べます。外部を選んでいた人は、更新後も EPUB が外部のまま引き継がれます。
    初回ウィザードで選んだ開き方は両方に入ります。
  - 外部ビューアの割り当てに「**電子書籍**（.epub）」の行を加えました。これまで EPUB は「テキスト」に入っていて、
    PDF と同じアプリしか選べませんでした。未設定なら「デフォルト」の設定で開きます。
- **★ オフライン（ダウンロード済み）の本も外部ビューアで開ける**ようになりました（外部を選んでいる形式。動画も含みます）。
  外部ビューアには管理用の番号ではなく**本の題名**のファイル名で渡します（ハードリンクなので容量は増えません）。
  内蔵ビューアでページ数が取れなかった本は、ローカルと同じく外部ビューアで開きます。
- **設定に「内蔵ビューア」タブを新設しました。**「表示」タブにあった内蔵ビューアの設定と、「ビューアキー」タブの
  キー割り当てをここにまとめています。キー割り当ては節ごとに折りたため（既定は閉じた状態）、既定から変えたキーの数を
  節ごとに表示します。設定の窓は画面の高さを超えないようになりました。
- 上流 [Washi](https://github.com/shunnag/Washi) を 1.17.0 から **1.21.0** へ更新しました。
  - 1.21.0 には、StackNest の開発中に見つけて上流へ送った修正 2 件が入っています。章の切れ目や画像ページで
    一瞬白くなる・文字の改行位置が表示後にずれる・画像が引き伸ばされてから合い直す、といったちらつきの解消
    （[#9](https://github.com/shunnag/Washi/pull/9)）と、画像 1 枚の項目（表紙・挿絵）を余白なしで表示すること
    （[#10](https://github.com/shunnag/Washi/pull/10)）です。取り込んでくださった [shunnag](https://github.com/shunnag) さんに感謝します。
  - 1.19.0 で本全体のページ数の計測が正確になり（時間切れになった章を 1 ページと数えていました）、1.18.0 で壊れた
    ZIP や深い入れ子の XML に対して落ちにくくなっています。
  - **1.18.0 から、同じ名前のファイルを 2 つ含む EPUB は開けなくなりました**（上流の安全対策）。

### Fixed

- **EPUB の窓が位置と大きさを覚えていませんでした**（毎回画面の中央に既定の大きさで開いていました）。
- **次の巻を開けないのに「次の巻なし」と出ていました。**次の巻のファイルが無いときは「次の巻を開けません
  （ファイルが見つかりません）」、リモートで共有が止まった・ライブラリが閉じられたなどで取れないときは
  「次の巻を開けません」と出すようにしました。今の本はそのまま読めます。
  - **EPUB の窓**は、次の巻のファイルが無いと窓を閉じてから「見つかりません」と出していました。窓を閉じずに留まります。
    フォルダへのアクセス許可が要るときは、許可のダイアログを出すため従来どおり開き直します。
- **ダウンロード済みのリモートの EPUB を、サーバに届かない状態で開くと、サーバの読書位置を本の先頭で上書き**する
  ことがありました。ページを動かすまでは位置を保存しないようにしました。
- リモートの本の「表紙を編集」で、候補の取得の待ち時間を 60 秒に延ばしました（大きなアーカイブではサーバが全体を
  読むため）。失敗したときは理由がログに残ります（これまでは何も出ずに候補が空のままでした）。
- 共有サーバが送る画像のうち、**EXIF の向きの情報（Orientation）を持つもの**の扱いを直しました。縮小の要らない
  大きさでも回転を反映してから送り、縦横が入れ替わる向きの画像が縮小されずに送られることもなくなりました。
- iPhone のホーム画面に追加した Web アプリで、iOS 27 の上端のぼかしが画面の上部を大きく覆っていた点を軽減しました
  （iOS 自身の効果のため、スクロールしたときに上端がわずかにぼけるのは残ります）。

### 既知の制限

- **EPUB はまだベータです**（混在本の挿絵は見開きにならない、本文検索・目次がない など。詳しくはヘルプの
  「EPUB を読む（ベータ）」）。
- **この版の画面とヘルプはほぼ日本語だけです。**英語の画面とヘルプは v0.16.0 で対応する予定です。
- リモートの EPUB を、サーバに届かない間に読み進めた位置は、あとでサーバへ送り直しません。

## [0.14.4] - 2026-09-12 — EPUB の操作を内蔵ビューアと同じキーで（Phase G51）

> EPUB の窓のキー操作が内蔵ビューアと別物だった点をまとめて揃えました。
> `Home` / `End` が章の端までしか動かなかった不具合も直っています。

### Added

- **★ EPUB の操作キーが内蔵ビューアと共通になりました**（G51）。設定 ▸ ビューア ▸ キー割り当ての**同じ表**が
  EPUB の窓にも効き、**割り当てを変えれば EPUB でも変わります**。ヘルプ（`?` / `h`）も窓の中に出ます
  （EPUB で意味のない項目は伏せます）。
  - **`Home` / `End` が本の最初・最後へ**飛ぶようになりました（これまでは章の先頭・末尾まで）。
  - **数字キーで本全体の割合ジャンプ**（`0`〜`9`）。
  - **`d` で見開き / 単ページの切り替え**、**`+` / `-` / `=` で文字の大きさ**（`=` で等倍に戻ります）。
  - **`f` で全画面**。設定に「**全画面で開く（EPUB）**」を追加しました（画像とは別に選べます。既定は OFF）。
  - **`s` で自動送り**（間隔と「最後のページの次」の設定に従います）、**`[` / `]` で前後の巻**へ。
- 上流 [Washi](https://github.com/shunnag/Washi) を 1.16.1 から **1.17.0** へ更新しました。縦組みの**縦中横**
  （数字が横に寝る表示）が効くようになり、章送りが速くなっています。

### Fixed

- **オフラインに保存した本のファイル名が、元の拡張子と違っていました。**保存時にファイルの先頭バイトから形式を
  推定していたため、EPUB・CBZ・RAR・7z・動画がすべて `.zip` として保存されていました。サーバが返す
  **元の拡張子を使う**ようにし、**すでに保存済みのファイルは起動時に正しい名前へ直します**（再ダウンロードは不要です）。
  - この影響で、**ダウンロードした EPUB がオフラインでは EPUB として開けませんでした**。直っています。
- EPUB の窓で `⌘1`〜`⌘5`（レート）などの**メニューのショートカットが効かず**、別の動作になっていました。
- EPUB で最後のページからさらに送ったとき、「最後のページの次」の設定が効いていませんでした（内蔵ビューアと揃えました）。

### 既知の制限

- EPUB の窓の下に出るページ番号は**章の中の番号**です（本全体の番号ではありません）。本全体の進捗表示は次の版で対応します。
- 本全体のページ数を測り終える前に割合ジャンプを押すと、章単位の近い位置へ飛びます。測り終えた後とは飛び先が変わります。

## [0.14.3] - 2026-09-08 — 動画のサムネイル（自動フレーム＋手動シーン選択・Phase G50）

> 5ch のスレッドでいただいた「動画ファイルのサムネイル生成」の要望から作りました。
> 取り込み時に自動で 1 枚拾い、気に入らなければシークバーで場面を選び直せます。

### Added

- **★ 動画に表紙が付きます**（G50・**MP4 / MOV / M4V**）: 取り込み時に**尺の 10% → 25% → 50%** の順にフレームを作り、
  **真っ黒・単色でない最初のもの**を表紙にします。冒頭が黒画面やロゴで始まる動画でも中身が見えます。
  - **場面を選び直せます**: 表紙の右クリックに「**動画からシーンを選ぶ…**」を追加しました。
    シークバー付きのプレイヤーで頭出しして確定します。
  - **選んだ場面は記録されます**。「表紙を再生成」で**同じ場面**を作り直せます（外部画像を表紙にした場合との違いです）。
  - **リモート書庫や Web リーダーの一覧にも**動画の表紙が出ます（シーンの選び直しはローカルの本のみ）。
  - **MKV / WebM / AVI は対象外**です。AVFoundation が開けないため、従来どおり表紙なしで取り込まれます。
  - 動画には「表紙を編集」（アーカイブ内のページ選択）が無いので、無効にしています。

### Fixed

- 手動で選んだ場面から「**自動に戻す**」または**取り消し**をしたとき、DB は自動に戻るのに
  **ディスク上のサムネイルが選んだ場面のまま**残っていました。形式に依らず作り直すようにしたので、
  PDF・EPUB・単独画像でも同時に直っています。
- 動画のフレーム生成に**上限時間がありませんでした**。20 秒で打ち切り、取り込み全体が止まらないようにしました。
  - 候補 3 点が 1 つの生成器を共有しているため、**先に成功した候補の期限が、まだ処理中の次の候補を巻き添えで
    取り消す**問題も直しました（復号に時間のかかる動画で、正常なのに表紙を作れなくなる）。
- 場面の明暗判定が**暗い場面を軒並み「真っ黒」と誤判定**していました（色空間の取り違え）。

### 既知の制限

- **動画のサムネイルはトリミングできません。**クロップの UI は「表紙を編集」の中にあり、動画ではその項目を
  無効にしているためです。別途対応します。

## [0.14.2] - 2026-09-08 — Washi 1.16.1（上流がキー入力のクラッシュを修正）

> `0.14.0` の EPUB 対応で見つけて[上流へ報告](https://github.com/shunnag/Washi/issues/3)していた不具合を、
> 作者の [shunnag](https://github.com/shunnag) さんが [Washi 1.16.1](https://github.com/shunnag/Washi/releases/tag/1.16.1) で
> 直してくださいました。StackNest 側に置いていた回避策を外し、上流の修正で通す形に戻します。報告への対応に感謝します。

### Changed

- **Washi を 1.16.1 に更新し、キー入力の回避策を外しました**: Washi の窓で `-`・Esc・`+`（⌘なし）を押すと
  落ちる不具合（`EPUBReaderView.keyDown` と WebView の無限再帰）を上流が修正したので、StackNest 側で
  キー処理を切っていた回避策（`handlesKeyboardNavigation = false`）を撤去しました。
  - 回避策の間は、**StackNest が扱わないキーが responder チェーンへ上がらず握り潰されて**いました
    （コンテナがキーを delegate へ渡した時点で止まり、上位へ流していなかったため）。既定へ戻したことで
    こうしたキーは通常どおり上位へ流れます。
  - ページ送りの優先権は従来どおりネイティブのキー監視が持ちます（Washi の JS が扱う矢印・Space・
    PageUp/Down・Home/End は、すべて監視側が先に消費するため二重には効きません）。

## [0.14.1] - 2026-09-07 — Stackroom 書庫の取り込みを直す（Phase G49）

> 外部の利用者 [@gsugawara](https://github.com/gsugawara) さんから、[PR #2](https://github.com/shelfsmith/stacknest/pull/2) で
> 2 件の不具合の報告と修正案をいただきました。**どちらも実在する不具合**で、こちらの書庫では条件に当てはまらず気付けていませんでした。
> 報告と修正案に感謝します。取り込みにあたって、いただいたコミットは作者名を残したまま取り込み、その上で
> 直す層と取りこぼしの扱いを見直しています。

### Fixed

- **★ Stackroom の書庫を取り込むと、本のパスが空になることがある**（G49）: Stackroom は初期に登録した本に
  `Path` を書かないことがあり、その本は場所を失ったまま取り込まれていました。`Path` が無い本は
  **表紙の取得元パスから場所を復元**します。復元したときは取り込みの警告に件数を出します。
  - 表紙がアーカイブ本体（zip / rar / 7z / PDF / EPUB）を指していればそれを、フォルダ内の画像を指していれば
    その親フォルダを使います。ただしアーカイブと分かっている本、Stackroom 自身のサムネイル置き場、
    相対パスは対象にしません（誤った場所を掴むより、空のままにして再リンクで直せる方を選びます）。
- **★ Stackroom の書庫を取り込むと、シェルフが 1 つも入らないことがある**（G49）: プレイリストの一覧を
  まとめて読んでいたため、**壊れたプレイリストが 1 つあると全シェルフが消えて**いました。
  1 件ずつ読むようにし、読めなかった 1 件だけを飛ばして残りは取り込みます。
  - `ItemView` / `ToolTab` が真偽値でも整数でも読めるようにし、`Items` や `Type` が無くても落ちません。
  - 棚の中身（`Items`）に整数でない要素が混じっていても、読める分は残します。
  - スマートシェルフの条件が読めなかったとき、`Playlists` が配列でなかったときも、黙って捨てずに警告に出します。
- 取り込みの警告が**どこにも表示されていませんでした**。CLI は標準エラー出力に出し、アプリからの取り込みはログに残します。

## [0.14.0] - 2026-09-06 — EPUB 対応（ベータ）、メタデータからのファイル名変更を CLI / MCP へ、表示の整え（Phase G44–G48）

> `0.13.0` から 8 フェーズ・105 コミット。**目玉は EPUB 対応**で、これは**ベータ**として出します
> （読めますが、下記の「ベータの理由」の範囲で挙動が固まっていません）。EPUB の解析と Mac での表示には
> [Washi](https://github.com/shunnag/Washi)（MIT）、Web リーダーには [foliate-js](https://github.com/johnfactotum/foliate-js)（MIT）を
> 使わせてもらっています。

### Added

- **★ EPUB を読む（ベータ）**（G48-1〜G48-4）: 取り込み時に表紙とメタデータ（題名・著者・言語・綴じ方向）を EPUB から取り、
  ダブルクリックで読めます。
  - **Mac**: 専用の窓で縦組み・ルビ・見開き・右綴じ。文字倍率は ⌘+ / ⌘− / ⌘0（JIS 配列でも）で、設定として保存されます。
    ← → Space PageUp/Down ↑ ↓ Home End でページ送り。読書位置（章＋進行率）は本ごとに保存されます。
  - **全ページが画像の EPUB（漫画）** は EPUB リーダーではなく**既存の画像ビューア**で開きます（見開き・右綴じ・ズーム・ルーペ・
    巻送りがそのまま効きます）。
  - **Web リーダー**（ブラウザ・iPad / iPhone）: テキストの EPUB は foliate-js で縦組み・ルビ・右綴じ、文字倍率は端末ごとに保存。
    画像本は従来のページリーダー。
  - **リモート書庫（別 Mac の StackNest から）**: テキスト EPUB は本体を取得してキャッシュし、ローカルと同じ窓で。
    ダウンロード済みならオフラインでも読めます。
  - **読書位置は Mac・Web・リモートで共有**されます（章＋進行率を正とし、エンジン固有の位置は同じエンジン同士でだけ使う）。
  - **綴じ方向は取り込み時に本へ反映**します。EPUB が宣言していれば（`page-progression-direction`）それを、宣言が無くても
    縦書き CSS 等から確定できればそれを入れます。手がかりの無い本には書かず、ユーザーのグローバル既定（右綴じ）に従います。
    右クリックでの上書きは従来どおり優先されます。
  - **ベータの理由**: 混在本（本文＋挿絵）の挿絵は見開きになりません。本文検索・目次・注釈はありません。Kobo 向け CSS を持つ本など、
    レイアウトに違和感が出る EPUB があります。Web の EPUB はオフライン保存できません。Mac リモートで読んだ位置とオフラインで読んだ
    位置は同期しません。
- **★ メタデータからファイル名を変える処理を CLI / MCP から**（G47）: `stacknest-cli rename-files` と MCP ツール `stacknest_rename_files`。
  GUI の ⇧⌘R と**同じ判断**（衝突・空の名前・255 バイト超は見送り）を共有します。トークンに **`@series` / `@volume` / `@keywordC`** を追加し、
  巻数は**シリーズ内で桁を揃え**ます。施錠庫はライブラリトークンがあれば操作できます（他の API と同じ扱い）。
- **列を表示に切り替えると、その列へ自動でスクロール**します（G44）。メニュー・ヘッダの右クリック・設定のどこから切り替えても同じです。
- About（StackNest について）・ヘルプ・README に、使用しているオープンソースソフトウェアのクレジットを載せました。

### Changed

- **グリッド表示をローカルとリモートで統一**（G45 / G46）: 表紙は 2:3 の枠に角丸と影、未読の印は表紙の右下、著者行は両方に出ます。
  枠自体は透明で、印・影・選択枠は表紙画像に付きます。
- Washi を 1.16.0 に更新（G48-4）。上流が画像ページの描画不具合（上流 Issue #1）を直したので、StackNest 側の回避策を外しました。

### Fixed

- 改名: 全トークンが空の本を改名すると拡張子だけの隠しファイルになる／255 バイトを超える名前で失敗する／失敗のたびにダイアログが出る、
  の 3 件（G47）。`BookRow` → `BookRecord` の変換でシリーズ・巻数・表紙名が落ちていた。
- リモート書庫の一覧をリロードすると横スクロール位置が先頭に戻る（G44）。
- EPUB（G48 の smoke で潰したもの）: 表紙や挿絵が白紙になる／リサイズで画像がはみ出す／JIS 配列で ⌘+ が効かない／
  Washi 1.16 の窓で `-`・Esc・`+` を押すと落ちる（上流 Issue #3。ホスト側で回避）／文字倍率の既定が半分になる（Web）／
  存在しない章を指す保存位置で白画面になる（Web）。

### Security

- リリース CI の個人情報リーク検査パターンをリポジトリに書かず Secrets から読むようにし、公開履歴を書き換えて残っていた識別子を除きました。
  gh の書き込み操作は公開名義でしか通らない番人（`Scripts/gh-guarded.sh`・pre-push フック）を入れました。

## [0.13.0] - 2026-08-27 — Finder タグとの双方向同期、ルーペ、お知らせの作り直し（Phase G37–G42）

> `0.12.1` から 6 フェーズ・74 コミット。**目玉は Finder タグ同期とビューアのルーペ**の 2 つで、
> どちらも 5ch スレッドでいただいた要望から作りました。

### Added

- **★ Finder タグと庫のメタデータを双方向に同期**（G39）: 庫ごとに項目を 1 つ選ぶと、
  macOS の Finder タグとその項目が**双方向に**同期されます。Finder で付けたタグは庫へ、
  庫で編集した値は Finder へ。**どちらかで消したタグは、もう一方からも消えます**
  （前回同期した値を覚えておく 3 方向マージ。単純な合併だと、消したタグが復活してしまいます）。
  - 契機は**庫を開いたとき**と**手動の再照合**（ライブラリ窓のツールバー、または庫の設定）。
  - **規模に依存しません。** Spotlight でタグの付いた項目だけを引くので、12,000 冊の庫でも
    実測 0.4 秒です。全ファイルの拡張属性を読むことはしません。
  - **タグの色は保たれます。** StackNest 側に色の概念はありませんが、既存の色は消しません。
  - **同期できる項目は 6 つ**（ジャンル / 作者 / ネタ / キーワード A・B・C）。
    **シリーズは同期できません** —— 単一値の項目なので、`Love, Chunibyo & Other Delusions` の
    ような区切り文字を含む名前が Finder 上で 2 つのタグに分裂し、片方を消すと値が半分になります。
  - 索引が無効なボリューム（`mdutil` で確認できます）では **Finder → 庫の方向が動きません**。
    その旨を知らせます。**黙って同期されないより、できないと分かるほうがよい**という判断です。
  - 名前に `, ` を含む Finder タグは、区切り文字と衝突するため同期対象から外し、
    その旨を知らせます（黙って壊すよりスキップします）。
- **★ 内蔵ビューアのルーペ**（G38 / G40）: ページの一部を拡大して見る虫眼鏡です。
  ビューアのキー設定から好きなキーに割り当てられます。
  - **倍率をスクロールで変えられます**（トラックパッドは無段階、ホイールは段階的）。
  - **形（円 / 正方形）と大きさ（小 / 中 / 大）**を設定から選べます。
  - 拡大部分は**その場で再デコード**するので、拡大しても粗くなりません。
- **CLI / MCP から Finder タグの再照合ができます**（G39）: `stacknest-cli finder-tags`
  （`status` / `set` / `resync`）と、対応する MCP ツール。アプリのメニューと**同じ処理**を
  呼ぶので、施錠中は走りませんし、二重には走りません。127.0.0.1 限定です。

### Changed

- **取り込み失敗の知らせ方**（G41）: 監視フォルダの自動取り込みの結果が、これまでは
  5 秒で消えるだけでした。**失敗があると自動で消えなくなり**、「詳細」から
  **どのファイルがなぜ失敗したかが見られます**（これまで知る手段がありませんでした）。
  × でいつでも閉じられます。**成功しただけの報告は、未読の失敗を押しのけません。**
- **ツールバーのラベル**（G42）: 右クリック →「アイコンとテキスト」で名前が出ない項目が
  ありました。全項目に出るようにし、あわせて読み上げ用の名前も追加しています。
- **設定の保存が粘るようになりました**（G37）: 長いドラッグの最中でも 2 秒ごとに書き込むように
  しました（従来は「最後の変更から 500ms」なので、ドラッグを続ける限り一度も書かれませんでした）。
  リモート設定データベースは `SQLITE_BUSY` を 2 秒待ちます。

### Fixed

- **`stacknest-cli library close` が閉じないことがある問題**（G39 の副産物）:
  「閉じました」と成功を返すのに閉じず、一度そうなるとアプリを再起動するまで
  その庫を閉じられませんでした。**閉じたことを確認してから成功を返す**ようにしています。
- ルーペがページ送りやレイアウト変更で消える／位置がずれる問題（G38）。
- 庫を閉じるとき、監視フォルダの停止を設定の書き出しより先に行うようにしました（G37）。

### 開発上の記録

- 実機 smoke は G39・G40・G41 の 3 本。**Codex レビューは G39 で 9 巡**し、
  7 巡で実際の欠陥を検出しました（深刻度は「庫全体のタグ喪失」→「1 冊」→
  「複数ボリュームの庫のみ」と下がっています）。
- テストは `swift test` **2140**（`0.12.1` 時点 2046）／App 層 **178**（同 116）。

## [0.12.1] - 2026-08-14 — ドラッグ操作の軽量化と、取り込み中断時の後始末（Phase G34–G36）

> `0.12.0` 以降のメンテナンス 3 フェーズをまとめたものです。新機能はありません。
> **効くのは、蔵書ファイルの破損チェックを走らせながらアプリを操作するときと、
> 列幅 / グリッドサイズ / ウィンドウのドラッグ操作です。**

### Changed

- **破損チェック中のもたつきを解消**（G34）: 走査を専用スレッドの低優先 I/O 下へ移し、
  ファイル読み込みがバッファキャッシュを押し流さないようにしました。走査中のサムネイル再読込は
  **71.8ms → 0.09ms**。走査そのものも 1 冊あたり 1.41 → **1.35 秒**と速くなり、
  993 冊の判定結果は変更前と完全に一致します。
- **監視フォルダの走査をメインスレッドの外へ**（G35）: ウィンドウのアクティブ化やスクロールの
  引っかかりが軽減されました。メインスレッドの実働は **2.7〜2.8% → 0.0%**。
- **列幅 / グリッドサイズ / ウィンドウ位置の保存をまとめ書きに**（G36）: これらはドラッグ中
  ずっと発火するため、1 回のドラッグがディスクへの書き込み数十〜数百回になっていました。
  操作が終わってから 1 度だけ書くようにしています。**外付け HDD や暗号化ディスクイメージに
  ライブラリを置いている場合に特に効きます。**

### Fixed

- **巻送りした本の既読が一覧に即反映されない**（G34 / G35）: リーダーで次の巻へ進んだとき、
  データベース上は既読になるのに一覧の表示が変わらず、開き直すまで反映されませんでした。
  ローカル / リモートの両方で修正しています。
- **取り込みの中断時に中途半端な本が残ることがある**（G36）: 監視フォルダの取り込み中に
  ライブラリを閉じると、走査が最後まで走り切って**閉じたデータベースへ書きに行って**いました。
  1 冊の切れ目で止まるようにし、途中まで取り込んだ分はそのまま残します
  （中断を理由に、取り込めた本を捨てることはしません）。
- **リモートライブラリの列幅 / グリッドサイズが保存されないことがある**（G36）:
  リモートのウィンドウは設定の保存経路がローカルと別で、アプリ終了時の書き出しが
  届いていませんでした。複数のリモートウィンドウを開いている場合に、
  **古い値が新しい値を上書きしうる**問題もあわせて修正しています。

### Internal

- RAR 書庫のテスト用検体をリポジトリへ収録し、CI でも RAR 経路が検証されるようにしました
  （`rar` コマンドが CI に無いため、これまでスキップされていました）。
- 自動テストは `swift test` **1978 件** / アプリ層 **115 件**（`0.12.0` 時点は 1904 / 90）。

## [0.12.0] - 2026-08-08 — 共有トークンによる権限分離、CLI / MCP 自動化、リモート運用の仕上げ、表示性能とセキュリティ（Phase 4.2d–4.2f / C / G3 / G4 / G12b / G15–G22 / G27 / G29）

> プレリリースとして配布: `v0.12.0-rc.1`（2026-07-07）/ `v0.12.0-rc.2`（2026-07-18）/ `v0.12.0-rc.3`（2026-07-25）。
>
> **rc.3 以降に加わったもの:** **蔵書ファイルの破損チェック**（専用ウィンドウ / CLI / MCP、リモート対応）と、**ライブラリのロックに関するセキュリティ修正**（変更にも現パスワードを要求）。ロックを使っている場合は rc.3 以前からの更新を推奨します（下記「Security」）。0.11.0（Phase 4.2）以降に積み上がった変更をまとめています。共有の権限分離（トークン）、コマンドライン / AI エージェントからの操作、フォルダ監視の自動取込、リモート閲覧のディスク永続キャッシュ、外部画像の表紙設定に加え、リモート運用の仕上げ（監視フォルダタブ、admin メンテナンス、取り消し / やり直し）と内蔵ビューアの安定化などが加わりました。
>
> **rc.3（2026-07-25）で加わったもの:** 内蔵ビューアのページ送りを CPU アーキテクチャ別に最適化（Intel 機で送れない問題の根治と、Apple Silicon での引っかかり解消）、Web リーダーの操作感（ドラッグめくり、空間ナビゲーション）、監視フォルダのサブフォルダ取込モード、1 冊単位の表紙再生成、リモート削除の進捗とキャンセル、そして**サーバ / リモート接続まわりのセキュリティ強化**。共有機能を使っている場合は rc.2 以前からの更新を推奨します（下記「Security」）。

### Added

**共有、権限**
- **共有トークン（アクセスティア）**: 相手ごとに **read / edit / admin** の権限と、見せるライブラリ（scope）を分けたトークンを発行できます。サーバ設定にトークン一覧（名称変更）、**トークン別の QR / URL** を追加。
- **トークンのライブ反映**: トークンの発行 / 名称変更 / 再生成 / 無効化が**サーバ再起動なしで即時反映**されます（旧トークンは即失効）。
- **リモートで本を削除（admin）**: 管理トークン接続時、リスト / グリッドの右クリックからリモートのまま「ライブラリから削除」「ゴミ箱へ」。

**自動化（CLI / MCP / API）**
- **ヘッドレス CLI `stacknest-cli`**: ローカル / リモートのライブラリをコマンドラインで操作（`libraries` / `list`（フィルタ、ブラウズ、ソート、scope、fields）/ `add` / `rm` / `set` / `detail` / `facets` / `shelves` / `me` / `shelf` CRUD / `watch` / `lock`（パスワードは stdin）/ `import` / `relink` / `dedup` / `unlock` / `grant` / `stamp` / `label`）。アプリに同梱し、ローカルアクセス用の使い捨てトークンで接続。
- **MCP サーバ `mcp-stacknest`**: 上記 CLI を包む Model Context Protocol サーバ。AI エージェントからライブラリの閲覧、編集、取り込み、共有トークン管理などを実行できます。
- **OpenAPI 3.1 + Redoc**: ローカルエンドポイントを API 専用化し、`/` に Redoc の API ドキュメントを同梱。

**取り込み**
- **監視フォルダ（自動取込）**: 指定フォルダを監視し、置かれたアーカイブ / 画像フォルダを自動でライブラリへ取り込み（フォルダ毎の命名プリセット、初回プレビュー、取込要約バナー）。
- **per-library 取り込み設定**: 自動分類（bookType）、厚さ閾値をライブラリごとに上書き（「StackNest 設定の値に従う」/ 個別指定）。設定に「取り込み」タブを追加。

**リモート閲覧の高速化、追従**
- **リモートリーダーのディスク永続キャッシュ**: リモート閲覧のページ / 表紙を端末にディスク永続キャッシュ（LRU + 可視保護 + TTL）。**再接続 / 再起動後もキャッシュが効きます**。設定「リモートキャッシュ」で上限 / 保持期間 / 使用量 / クリアを管理。
- **先読みの Web パリティ + 全冊先読み**: 内蔵ビューアの先読みを「前方優先 + スキップストライド」に刷新。放置で本 1 冊分をキャッシュする**全冊先読み（tier3）トグル**（既定オフ）。
- **キャッシュ カバレッジ帯**: リモートビューアのプログレスバーに、L2 キャッシュ済みのページ範囲を淡色帯で可視化。
- **表紙キャッシュのサーバ追従（G4c）**: サーバの版トークンで表紙キャッシュを版鍵化し、リモート閲覧クライアントが**サーバ側の表紙変更へ一覧リロード / 再接続で追従**（手動キャッシュ削除が不要に）。ホストアプリはリモート由来の表紙変更を開き直さず反映。

**表紙**
- **外部画像を表紙に（ローカル G4a / リモート G4b）**: 詳細ペインの表紙へ**画像をドラッグ&ドロップ**、または「外部画像を表紙に設定…」メニュー→クロップで、任意の外部画像を表紙に設定。**見た目のみの変更でアーカイブ本体は不変**。RW トークンのネイティブリモートクライアントでも利用可（サーバに `PUT /books/:id/cover-image` を追加）。

**リモート編集、Web**
- **レート / 未読を R トークンでも編集**: 共有評価 / 閲覧状態として、閲覧のみのトークンでもレート、未読を変更可（サーバ同期）。
- **サーバ同期のラベルカスタマイズ（リモート）**: リモートの詳細 / ファセット / スタンプ表示がサーバ同期のカスタムラベルを反映。RW でラベル編集も可能。
- **Web リーダー巻末ナビ + PWA**: Web リーダーに巻末ダイアログ（次の巻へ / 先頭へ / 閉じる）、favicon / apple-touch-icon / PWA manifest を追加。
- **起動時オプション**: 「前回開いていたライブラリ」で**複数窓を全復元**、起動時にリモート共有を**自動 ON** にするオプション（著作権同意ゲート付き）。

**リモート運用の仕上げ（G12b）**
- **監視フォルダのタブ化 / サブフォルダ取込モード修復**: リモート設定シートの監視フォルダ編集を「取り込み」タブに統合。保存時にサブフォルダの取込モード（subfolderMode）が失われる不具合を修正。
- **管理者メンテナンス（進捗表示付き）**: メタデータの補完、表紙の圧縮を、進捗バー付きの非同期ジョブとしてリモートから実行可能に。
- **リモートの取り消し、やり直し（⌘Z / ⌘⇧Z）**: メタデータ編集、削除をリモートから元に戻す / やり直せるように。
- **命名フォーマットプリセットのリモート編集**: プリセットの追加、変更、削除を対応ティアからリモートで実行可能に。
- **監視フォルダの既存ファイル一括再取込**: 「既存も取り込む」で、監視開始前からあったファイルもまとめて取り込み。

**内蔵ビューアの安定化（G15）**
- **同一本の重複起動を防止**: ローカル、オフライン、リモートいずれでも、読み込み中の同じ本を続けて開こうとしても複数ウィンドウにならないよう起動処理を刷新。設定に「**複数ビューアの起動を許可**」を追加（既定オフ＝別の本を開くと既存ウィンドウを再利用、オンで本ごとに別ウィンドウ）。
- **リモートグリッドの無表紙表示**: 表紙の無い本が読み込み中のまま回り続けず、すぐに無表紙アイコンを表示。
- **非対応形式の専用メッセージ**: リモートで動画、文書など内蔵ビューア非対応の形式を開こうとした際、汎用エラーではなくカテゴリ別のメッセージを表示。

**Web リーダーの操作感（G17 / G20）**
- **ドラッグめくり**: 指やトラックパッドの動きにページが 1:1 で追従し、離すと慣性で送り先が決まります。端では引っ張り抵抗（ラバーバンド）、確定時に触覚フィードバック。
- **空間ナビゲーション**: 一覧 → 本 → リーダーの移動をスライドで表現し、戻ると**元のスクロール位置と絞り込みを保ったまま**一覧へ戻ります。
- **半透明マテリアルと押下フィードバック**: 上部バーなどに半透明素材を採用。「視差を減らす」「透明度を下げる」「コントラストを上げる」の各アクセシビリティ設定に対応します。

**取り込み（G9b）**
- **サブフォルダの取込モードを 3 択に**: 監視フォルダごとに「サブフォルダを取り込まない」「サブフォルダを 1 冊として取り込む」「サブフォルダの中も個別に取り込む」を選べます。

**表紙（G21）**
- **1 冊単位の表紙再生成**: これまでライブラリ全体のジョブしかなかった表紙の作り直しを、本ごとに実行できます。**再リンク時は表紙とページ数も自動で更新**されます。

**リモート運用（G22）**
- **リモート削除の進捗とキャンセル**: 複数冊をリモートから削除する際に進捗バーを表示し、途中で中止できます（中止は残りの未処理分を止めます）。

**蔵書ファイルの破損チェック（G27 / G29）**
- **ファイルの破損チェック**: 蔵書のアーカイブが壊れていないかを検査します。File メニュー「ファイルの破損チェック…」から専用ウィンドウを開き、**未検査をスキャン / 全件やり直し / 破損のみ再検査**の 3 モードで実行できます。結果は本ごとに記録され、**前回は正常だったのに今回破損した本（劣化）を先頭に表示**します。
- 検査は 2 段階です。**簡易チェック**はファイルの有無・サイズ・開けるかを見ます。**詳細（CRC）チェック**はアーカイブ内の各エントリを実際に読んで CRC を検証するため確実ですが、蔵書規模によっては数時間〜数十時間かかります（**いつでも中断でき、途中まで の結果は保存されます**）。
- 対象は `.zip` / `.cbz` / `.rar` / `.cbr` / `.7z` と単独画像です。フォルダ・動画・PDF/EPUB 等は CRC を持たないため「対象外」として記録します。
- **リモートからも使えます**（G29）。別の Mac でリモートライブラリを開いているときも、同じウィンドウから閲覧・スキャン開始・中断ができます。長時間かかるジョブのため、**開始には管理者権限のトークンが必要**です（解錠済みかつ admin のときだけメニューが有効になります）。
- **CLI / MCP からも実行できます**: `stacknest-cli integrity scan / status / list / full-scan / job-status / cancel`。
- ウィンドウは独立ウィンドウで、スキャン中も他の操作やアプリ終了を妨げません。

### Changed
- **共有トークンに一本化**: 旧「アクセス / 編集トークン」UI を共有トークン一覧へ統合。ユーザー向け文言を「共有トークン」に統一。
- **ローカルアクセス設定をアプリ設定へ**: ローカル制御（CLI / MCP 用）の設定を共有窓からアプリ設定タブへ移設し、「ローカルアクセス」に改称。
- **設定タブ再編**: 一般 / 取り込み / 表示 / ビューアキー に整理（取り込みタブに自動分類 + 監視フォルダを統合）。
- **共有前の著作権警告**: サーバ開始前に著作権警告（以後抑止可）を表示。
- UI 表記「リモート / オフラインビューア」→「**リモート / オフラインブラウザ**」に統一。
- **無限スクロールのライブ同期を並列化**: リモートブラウザの追従取得をまとめて並列取得するよう変更し、体感を改善。
- **内蔵ビューアの描画を CPU アーキテクチャ別に最適化（G18 / G19）**: Apple Silicon では画像をフル解像度のまま遅延デコードし（縮小は描画時に GPU が担当）、Intel では背景スレッドで縮小デコードします。**両立する単一の方式が無いため適応分岐**としました。Intel 機でページを送れない問題と、Apple Silicon で数ページ先から重くなる問題の両方が解消します。
- **アーカイブの読み出しを 1 パス方式に（G18 / G19）**: ページを送るたびにアーカイブを開き直して先頭から探していた処理を、開いたまま順方向に読み進める方式へ変更。ページ数の多い本ほど効きます。
- **リモート閲覧のページキャッシュを高速化（G17）**: キャッシュ DB の書き込み方式を見直し、参照時刻の更新をまとめて行うように変更。
- **ビューアまわりの用語を統一（G17）**: 表記の揺れを 118 箇所で整理しました。

- **「整合性チェック」という同名の項目が 2 つあったのを改名しました**（G29）。File メニューは **「ファイルの破損チェック…」**（蔵書アーカイブの中身を調べる）、ライブラリ設定は **「データベースを検査」**（ライブラリの管理データを調べる）になりました。調べる対象が名前で分かります。
- 破損エントリの理由表示を日本語にしました（G31）。壊れていたファイル名はそのまま表示します。

### Fixed
- **リモート表紙の stale**: リモートで表紙を差し替えても、編集した詳細ペインや別の閲覧クライアントが古い表紙のまま残る問題を修正（表紙取得の HTTP キャッシュ〔`immutable`〕を差し替え時にバイパス / 表紙キャッシュをサーバ版で版鍵化 / ホストは per-book で反映）。
- **起動復元で閉じた窓が復活**する問題を修正（`NSWindow.willClose` 増分方式へ変更、終了フック非依存）。
- **複数窓での削除確認が 2 重表示**される既存バグを修正（通知のフォーカスガード）。
- **トークンの失効**: 再生成 / 無効化で旧トークンが失効しない、全トークン削除後に復活する問題を修正（既定グラントを現トークンへ同期、一度きり移行マーカー）。
- 内蔵ビューア / サムネイルに **EXIF 回転**を反映。監視パス不在の警告、ポート競合時の自動再採番。
- **セキュリティ**: CLI のパスワードを stdin 入力に（argv 露出回避）、MCP add の引数スマグリング防止（`--`）、ローカルエンドポイントの Web UI 撤去（API 専用化）。
- **リモートの取り消し（undo）が同時編集を上書きしない**: メタデータ編集の取り消しが、他クライアントの並行編集を巻き戻さず正しい編集前の値へ戻るように修正。
- **ゴミ箱へ移動した本の取り消しでファイルも復元**: リモートで削除した本を ⌘Z で戻すと、登録だけでなくファイルもゴミ箱から元の場所へ復元されるように修正。
- **巻送り後の重複ウィンドウを抑止**: 内蔵ビューアで巻を送った直後、同じ本が別ウィンドウで重複して開くことがある問題を修正。
- **絞り込み時に一覧が先頭へ戻るように修正**: 一覧を大きくスクロールした状態で検索、絞り込みを行うと元の位置に留まっていた問題を修正（編集後やライブ同期による更新では位置を保持）。
- **セキュリティ**: リモートの復元操作を悪用した任意ファイル移動を防止。
- 削除確認ダイアログの文言を、⌘Z で取り消せることを反映するよう整合（リモート削除の確認も含む）。
- 未設定（空欄）のタグ欄に値を入力した後の取り消し（⌘Z）で欄が正しく空へ戻るよう修正。

**再リンクと表紙（G4d / G21 / G22）**
- **再リンク後にページが開けなくなる問題を修正**: フォルダを本として扱っている場合、再リンク後も古いファイル情報が使われ続け、**目次は「6 ページ」と示すのに後半のページが 404 になる**ことがありました。
- **再リンク後も表紙とページ数が古いまま**だった問題を修正（再生成が自動で走るようになりました）。
- **表紙を差し替えても一覧が古い表紙のまま**残る問題を修正（外部画像どうしの差し替え、および自動表紙へ戻した場合）。
- **外部画像の表紙が失われた際の状態不整合を修正**: 自動表紙へフォールバックした後も内部的に「外部指定」の印が残り、「表紙を再生成」が使えなくなっていました。
- **表紙の圧縮ジョブが単独画像の本を対象外にしていた**問題を修正。

**リモート、Web（G20 / G21）**
- **Web の上部バーがスクロールで流れて消える**問題を修正。
- **Web でリーダーから一覧へ戻るとスクロール位置が先頭に戻る**問題を修正（10 分間保持。無限スクロールの深い位置にも追従します）。
- **クラシックスクロールバー環境で横スクロールバーが出る**問題を修正（スクロールバー幅を実測して差し引くようにしました）。
- **Web の見開き表示で 1 ページ目も見開きになる**問題を修正。アプリと同じく表紙は単頁で表示します。
- **閲覧のみのトークンで単頁 / 見開きを切り替えるたびに赤いエラーが出る**問題を修正（権限が無い場合の保存失敗は通知しません。表示の切り替え自体は従来どおり機能します）。
- **リモートから複数の本を削除すると「サーバエラー -1」が出る**問題を修正（削除自体は成功していました）。
- **管理者トークンでのリモート接続時、ローカル専用のメニュー項目が有効に見える**問題を修正。

**サーバ（G4d / G21）**
- **ブラウザのキャッシュが古いページを返し続ける**問題を修正。サーバ側でページの版を検証し、版が合わない要求の応答はキャッシュに残さないようにしました。
- **ネットワークストレージの一時的な不調でライブラリ全体が再ダウンロードになる**問題を修正（ファイル情報の取得に失敗した場合は保存済みの値を使います）。
- **強制終了時に残ったサーバの一時ファイル**を、起動時に 24 時間より古いものから一掃するようにしました。
- **コピー途中や空のフォルダが 0 ページの本として取り込まれる**問題を修正。

### Security

サーバおよびリモート接続まわりの脆弱性を修正しました。いずれも**共有トークンを第三者に渡している場合に影響**します。攻撃手法の詳細は記載しません。

- **閲覧権限のトークンで、共有フォルダ外の任意のファイルを読み出せる**問題を修正しました（再リンク先パスが検証されていませんでした）。あわせて、許可フォルダ内にフォルダ外を指すシンボリックリンクがある場合も検証をすり抜けないよう、パスを実体まで解決してから判定します。
- **共有トークンの発行 / 更新時に、自分より強い権限や広い公開範囲を与えられる**問題を修正しました（権限と scope の昇格を拒否します）。
- **公開範囲を限定したトークンで、他のライブラリの共有トークンを一覧取得できる**問題を修正しました。
- **施錠したライブラリのロックが「続きから読む」で迂回できる**問題を修正しました（ローカル / リモートの両方。ウィンドウを閉じた後は再度の解錠が必要です）。
- **解錠のパスワード試行が無制限だった**問題を修正しました（試行回数の超過でロックアウトします）。あわせて、公開範囲外のライブラリに対する解錠試行を拒否します。
- **細工したアーカイブでメモリを枯渇させられる**問題を修正しました（展開サイズに上限を設けました）。
- **ページ番号の指定でサーバを停止させられる**問題を修正しました。
- **オフライン保存の保存先がサーバの指定値のまま使われていた**問題を修正しました（保存先を検証します）。
- **リモート接続時に HTTP リダイレクトへ追従していた**のをやめました（認証情報の転送を防ぎます）。
- **共有トークンをコピーする操作で、トークンが不必要に露出していた**点を改善しました。
- **ライブラリのロック解除には現パスワードが必要なのに、パスワードの変更には不要**だった問題を修正しました（G27a）。解錠したまま席を離れると、第三者がパスワードを上書きしてライブラリを乗っ取れる状態でした。**ロックが既にある場合、変更・解除のどちらにも現パスワードを求めます**（新規に設定する場合は従来どおり不要）。HTTP 経由の変更・解除も同じ規則になりました（以前は管理者権限のみで通り、GUI より緩い状態でした）。
- ロックの変更・解除が「現パスワードを検証してから無条件に書き込む」形だった問題を修正しました（G27a）。検証した値と書き込みが結び付いていなかったため、並行する正当な変更を後から上書きしうる隙がありました。**検証した値と一致する場合にのみ書き込みます**。
- **応答サイズの上限**をクライアント側に設け、巨大な応答でメモリを枯渇させられないようにしました。
- 並び替えに使う列名を内部の許可リストで検証するようにしました。

## [0.11.0] - Unreleased — リモート共有 / ネイティブクライアント / オフライン / リモート編集（Phase 4.2）

> プレリリースとして配布中: `v0.11.0-rc.1`（2026-06-14、Phase 4.2b＝閲覧 / オフライン基盤）/ `v0.11.0-rc.2`（2026-06-21、Phase 4.2c＝同等化、リモート編集）。

### Added
- **ネイティブリモートクライアント**（4.2b-1b-2b）: 別 Mac の StackNest から共有サーバへ接続し、サイドバー / ファセット / フィルタ / グリッド、リスト / 詳細ペインでフルブラウズ + 内蔵ビューアで閲覧。読書進捗はサーバへ同期。サーバに `/shelves`、`/facets`、`/books/:id/detail`、`scope+filter+browse` 付き `/books` を追加。
- **オフラインダウンロード**（4.2b-2）: リモートの本をローカル保存（`OfflineStore` + `index.json`）し、**接続なし**で一覧、閲覧（続きから）。タイトル画面 / File メニューの「オフライン（ダウンロード済み）」から起動。
- **一括ダウンロード / 削除**（4.2b-5、4.2c-3）: リモート / オフラインで ⌘ / Shift による複数選択 → 一括 DL / 一括削除（ネイティブ複数選択へ統一）。
- **リモート / オフライン巻送り**（4.2b-4、4.2b-6）: 内蔵ビューアの前 / 次の巻がリモート（隣接巻ストリーム、未 DL 可）、オフライン（連続巻）でも動作。読みかけの巻は「続き / 最初」を選択。
- **リモート検索**（4.2c-3）: ツールバー検索（ライブフィルタ + 300ms デバウンス + クリア）。
- **リモート編集（RW トークン）**（4.2c-6a、4.2c-6b）: 詳細ペインでメタデータの単一 / 複数選択一括編集（進捗 N/M + 中断）、スタンプ付与 / 定義編集、**表紙ページ選択 / クロップ編集**、読み方向変更。スタンプ定義はサーバ常駐 canonical で同期、表紙はサーバ側で thumbnail 再生成。
- **リモートブラウザ状態の永続化**（4.2c-7）: ファセット / ソート / grid / フィルタ / サイドバーを (サーバ, ライブラリ) 単位で UserDefaults に保存（別ライブラリ非伝播）。
- **keywordC フルカラム化**（4.2c-6c）: ローカル / リモートのリスト列、ソートに keywordC を追加（リモート複数選択編集の現在値表示も対応）。

### Changed
- **リモートグリッドをローカル同等化**（4.2c-4）: ⌘ / Shift 複数選択、表示サイズスライダー、右クリック並び替え、ツールバー配置をローカルと統一。ローカルの「シリーズ」並び替えもシリーズ→巻数の 2 段ソートに統一。
- **リモートビューア進捗の二重同期**（4.2c-5）: リモート（DL 済み）とオフラインの進捗を双方向 write-sync し、開く時は最大ページで解決。
- README（日本語 / 英語）、アプリ内ヘルプを更新し、リモートの共有 / クライアント / オフライン / 編集（RW 編集対応）を記載。

### Fixed
- リモート / オフラインの多数の UX 修正（一覧の単一クリック選択、詳細ペイン幅固定、DL 中の中断 / 進捗 / 選択巻き戻り、ビューア続きから再開、「読んだ日」列の更新、Web 検索の IME 合成中の誤発火 ほか）。
- 表紙差し替え後にリモートグリッドが古い表紙のまま残る問題、リモートグリッドのクロップ未適用を修正（`coverVersion` / `coverCropRectJSON` を一覧 DTO に追加）。
- セキュリティ: サーバ応答から path を秘匿（漏洩防止）、読み取り専用時に「Finder で表示」を非表示、ファセット / browse 列識別子の allowlist 化（SQL injection 対策）。

## [0.10.0] - 2026-06-08 — ファイル名コピー & リリース整備（B24）

### Added
- **右クリック「ファイル名をコピー」（B24）**: グリッド/リストの本の右クリックメニューに追加。拡張子なしのファイル名をクリップボードへコピー（単一選択時のみ有効、複数選択、path 無しはグレーアウト）。

### Changed
- リリースを正式版として配布（prerelease 解除）。
- インストール手順を **macOS 15 Sequoia 以降の Gatekeeper 仕様**に合わせて更新（「右クリック →『開く』」は廃止 → システム設定の「セキュリティ」から「このまま開く」で許可、または `xattr` で quarantine 除去）。

## [0.9.0] - 2026-06-07 — ライブラリ保全 & DB 修復（Phase 2.8 / 2.9）

### Added
- **DB 予防保全（B22）**: 編集のあったセッションを閉じるたびに、バンドル内 `Backups/` へ世代バックアップを自動取得（SQLite Online Backup API、閲覧のみの無編集セッションはスキップ）。保持世代数はライブラリごとに 1〜20（既定 5）で設定可能。
- **整合性チェック（B22）**: ライブラリを開く際に `PRAGMA quick_check` を実行し、破損を検知すると「最新の正常なバックアップから復元しますか？」と案内。設定に手動の整合性チェック / 今すぐバックアップ / バックアップフォルダ表示ボタンを追加。
- **アプリ内 DB 修復 `.recover`（B23）**: バックアップからの復元ができない場合、「.recover で修復を試す」からシステムの `sqlite3 .recover` を実行して可能な範囲のデータを救出。救出できた本の件数を提示し、正常なら差し替えて開く（壊れた本体は `library.corrupt-*` / `library.prerecover-*` として保持）。
- **リンク切れ再指定（A19）**: 本体ファイル / フォルダの移動で参照切れになった本を再リンク。右クリック「ファイルを再指定…」（即時）と、メニュー「リンク切れを検出…」（個別列挙 + フォルダ一括再マップ）。
- **復旧ガイド**: `docs/recovery-guide.md` を追加（手動 `sqlite3 .recover` 手順、退避ファイルの説明）。

### Fixed
- **NFC 正規化**: macOS のファイル名（NFD）由来のシリーズ / タイトルが、入力、取り込み由来（NFC）と別文字列として扱われ、ブラウズのファセットが分裂したりフィルタで取りこぼす不具合を修正。保存時に NFC 正規化し、既存データは migration v16 で一括バックフィル。

## [0.8.0] - 2026-06-06 — ブラッシュアップ & 性能（Phase 2.7）

### Added
- **重複検出（A20 / B11）**: 別ディレクトリにある同一内容の本を SHA-256 のバイト一致で検出（加えて series+volume も一致）。解決シート（登録のみ削除 / ファイルもゴミ箱 / グループ無視）。
- **ラベルカスタマイズ（A22 / A23）**: 内容系フィールドと bookType の表示名をライブラリごとに任意の名前へ。全表示箇所（列ヘッダ、ソート、詳細、フィルタ、スタンプ、ブラウズ、スマートシェルフ）へ一貫反映。
- **ビューアキー再割当 UI**: ビューアの全操作キーを設定「キー」タブで自由に割り当て（競合は拒否、ヘルプ自動生成）。
- **複数命名フォーマットプリセット（B6）**: 命名フォーマットを複数保存し、リネーム時に選択。
- **配布**: CI（GitHub Actions）による ad-hoc 署名 Universal ビルドのリリース配布を開始（初の tag 付きリリース）。

### Changed
- **ソート性能最適化（B9）**: DSU + ICU sort key（`ucol_getSortKey`）で大規模ライブラリのソートを高速化（実スケール 5,000 件で起動時の体感を大幅改善）。

---

## 0.8.0 以前（フェーズ管理期、タグなし）

タグ付きリリース以前の開発履歴をフェーズ単位で記載します（**新しい順**）。各フェーズの詳細設計は計画リポジトリのスペックにあり、本リポジトリの README「ロードマップ」が要約です。
（早期フェーズの日付は計画リポジトリの最終 smoke / 決定ログに基づきます。2.2 は単独の完了記録が無く、関連 spec〜直後フェーズの smoke から 2026-05-03〜07 の完成と推定。）

### Phase 2.6g — 2026-06-04 — Keychain 非依存の生体認証ロック
- armedHash 方式で解錠経路から Keychain アクセスを排除（毎回プロンプトの根治、平文非保存）。

### Phase 2.6f — 2026-06-04 — ドキュメント & アプリ内ヘルプ
- README（日英） / CONTRIBUTING 整備、Help メニューを実装内ヘルプページ化。

### Phase 2.6e — 2026-06-04 — リブランド & 匿名化
- 識別子整理（`app.shelfsmith.stacknest`）と GitHub 移管（`shelfsmith`）。

### Phase 2.6d — 2026-06-04 — 安全性 & クリーンアップ
- マルチ Mac 同時オープン防止のロックファイル + 旧称クリーンアップ + アプリ内 Help / About。

### Phase 2.6c — 2026-06-03 — 初回起動ウィザード
- 内蔵 / 外部ビューア選択、内蔵初期設定、最初のライブラリ作成、設定から再表示。

### Phase 2.6b-2 — 2026-06-02 — 内蔵ビューア拡張
- 見開き表示、本ごとページ方向、スライドショー自動進行、続きから読む、巻末挙動（次の巻 / ループ）、全画面で開く、HEIC/HEIF/TIFF/AVIF 対応。

### Phase 2.6b — 2026-06-01 — 内蔵ビューア コア
- 専用ウィンドウ、全画面、フィット / ズーム / パン、ページ送り、PDF / アーカイブ / フォルダ / 単一画像。

### Phase 2.6a — 2026-05-31 — スマートシェルフ MVP
- 条件式コレクション（ルールリスト、動的評価、facet/FTS と AND 重ねがけ）。

### Phase 2.5k — 2026-05-29 — キーボードナビゲーション
- グリッド / リストの矢印移動、範囲拡大、Enter で開く、Home/End/PageUp/Down 統一。

### Phase 2.5g–2.5i — 2026-05-28 — 自動分類、表紙パイプライン、PDF（v0.6.5）
- bookType 自動分類の設定化（新規取込のみ、閾値設定、手動リラベル併用）。
- 表紙の crop UI（横長カバー対応、縦長正規化） + 表紙圧縮（保存時ダウンサンプル、全表紙再生成）。
- PDF 表紙取込、PDF ページ数認識（zip 内 PDF 含む）、単独 image/PDF/movie/text の追加。

### Phase 2.5f — 2026-05-27 — 「最近開いたライブラリ」経路集約
- 更新経路を 1 か所に集約（fixedLibrary 起動 / Finder ダブルクリックでも履歴更新）。

### Phase 2.5e — 2026-05-27 — 拡張子別 外部ヘルパー
- 拡張子別ヘルパーマッピング / スライドショー、Zip ヘルパー個別指定 / フォルダを外部ヘルパーへ渡す。

### Phase 2.5（コア）— 2026-05-24 — マルチライブラリ & ファイル操作
- 複数ライブラリ + 任意位置（Cmd+O、最近開いた一覧、macOS タブ）、ライブラリごとのパスワードロック + Touch ID、画像フォルダ書籍（zip 化なし）、ファイル移動 / リネーム（双方向トークン、プレビュー）、ファイル名パーサ修正、シリーズ + 巻数分離（migration v10）、表紙編集（D&D + zip 内ページ選択）、削除安全化（Undo）、詳細ペイン Tab ナビゲーション。

### Phase 2.4 — 2026-05-09 — 編集 & フィルタ
- 詳細ペイン編集（タグドロップダウン、メモ、bookType）、ツールバー 3 種フィルタ（未読 / レート / bookType）、ファセットフィルタ列（ブラウズ）、FTS5（trigram）全文検索、スタンプ表示、タグ「→」ジャンプ。

### Phase 2.3 — 2026-05-07 — ブラウザコア
- サイドバー（ライブラリ / お気に入り / 最近の項目）、手動シェルフ、詳細ペイン（表示）、インクリメンタル検索、リスト表示、レート / 未読 / bookType 設定（メニュー + ショートカット）、お気に入りへの D&D。

### Phase 2.2 — 2026-05-03〜07 — 表示の基礎
- グリッド / アイコン表示、サムネイル生成、アーカイブ（zip/cbz/cbr/7z）読み込み、外部ビューア指定。

### Phase 2.1 — 2026-05-02 — 互換インポータ（最初の土台）
- Stackroom のライブラリ XML を解析して SQLite（GRDB）へ取り込む CLI インポータ。データモデル（タイトル / 作者 / ジャンル / キーワード / 関連 / メモ / レート / 未読 / ページ数 / 表紙 / bookType）を確立。

[0.10.0]: https://github.com/shelfsmith/stacknest/releases/tag/v0.10.0
[0.9.0]: https://github.com/shelfsmith/stacknest/releases/tag/v0.9.0
[0.8.0]: https://github.com/shelfsmith/stacknest/releases/tag/v0.8.0
